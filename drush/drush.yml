#
# A Drush configuration file
#
# Docs at https://github.com/drush-ops/drush/blob/master/examples/example.drush.yml
#
# Edit or remove this file as needed.

command:
  site:
    install:
      options:
        account-pass: 'testing'
  # Currently not working. See https://github.com/drush-ops/drush/issues/2956
  config:
    import:
      options:
        partial: true
