{#
/**
 * @file
 * Theme override to display an embedded remote video.
 *
 * Available variables:
 * - name: Name of the media.
 * - content: Media content.
 *
 * @see template_preprocess_media()
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'cu-media',
    'media--embedded',
    'media--remote-video',
  ]
%}
<div{{ attributes.addClass(classes)}}>
{% if content %}
  {{ content }}
{% endif %}
</div>
