{% include '@union/_card.twig' with {
    heading: paragraph.field_heading.isEmpty() ? '' : content.field_heading.0,
    heading_element: 'h3',
    card_content: content|without('field_heading', 'field_link'),
    icon_data: cu_icon ? cu_icon : NULL,
    button_data: not paragraph.field_link.isEmpty() ? {
        text: content.field_link[0]['#title'],
        attributes: button_attributes,
        url: content.field_link[0]['#url']
    } : NULL,
    attributes: attributes.addClass([
        'cu-component',
        'cu-component--' ~ paragraph.bundle|clean_class,
        'cu-colorscheme--dark'
    ])
} only %}