{# `content` is rendered because it must be a string in the dismissible for sha1 #}
{% set rendered_content %}{{ content }}{% endset %}
{% include '@union/_dismissible.twig' with {
  content: rendered_content
} only %}
