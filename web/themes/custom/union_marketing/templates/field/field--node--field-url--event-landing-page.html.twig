{{- attach_library('union_organizer/button') -}}

{%- for item in items %}
  {% set link = item.content %}
  {% set link = link|merge({'#title': 'Register Now'|trans }) %}
  {{ link|merge({'#options': {'attributes': {'class': ['cu-button']}}}) }}
{% endfor -%}
